export enum Sex {
    MALE = 1,
    FEMALE = 2,
    UNISEZ = 3,
}

export enum SideOfFoot {
    LEFT = 1,
    RIGHT = 2,
}

export enum OutSole {
    TOE = 1,
    MIDDLE = 2,
    HEEL = 3,
}

export enum Age {
    ADULT = 1,
    YOUTH = 2,
}

export enum Role {
    ADMIN = 1,
}
